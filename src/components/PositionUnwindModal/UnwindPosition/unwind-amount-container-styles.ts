import { Box } from "@radix-ui/themes";
import styled from "styled-components";

export const TransparentUnderlineButton = styled(Box)`
  text-decoration: underline;
  cursor: pointer;
  font-size: 12px;
`