import styled from '@emotion/styled'
import {Card} from '@mui/material'

export const BaseCard = styled(Card)({
  height: '340px',
  background: 'rgba(32, 36, 49, 1)',
  position: 'relative'
  // [theme.breakpoints.down('md')]: {
  //   display: 'none',
  // },
})
